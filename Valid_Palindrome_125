class Solution:
    def isPalindrome(self, s: str) -> bool:
        s = ''.join(ch.lower() for ch in s if ch.isalnum())
        s1=s[::-1]
        if s==s1:
            return True
        else:
            return False
